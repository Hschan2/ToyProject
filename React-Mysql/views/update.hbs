<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    {{!-- <meta name="viewport" content="width=device-width, initial-scale=1.0"> --}}
    <link rel="shortcut icon" type="image/x-icon" href="https://icon.horse/icon/[url.hostname]">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>정보 수정</title>
</head>
<body>
    <div class="container">
        <header class="blog-header py-4 bg-white border-bottom">
            <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4 pt-1">
                    {{#if user}}
                    <a class="text-dark mx-2 font-weight-bold" href="/boardList">게시판</a>
                    {{/if}}
                </div>
                <div class="col-4 text-center">
                    <a class="blog-header-logo text-dark h1 font-weight-bold title" href="/">NodeBoard</a>
                </div>
                <div class="col-4 d-flex justify-content-end align-items-center">
                    {{#if user}}
                    <a class="text-muted text-dark mx-2" href="/profile">내 정보</a>
                    <a class="btn btn-sm btn-outline-secondary mx-2" href="/withdrawal">회원탈퇴</a>
                    <a class="btn btn-sm btn-outline-secondary mx-2" href="/auth/logout">로그아웃</a>
                    {{else}}
                    <a class="btn btn-sm btn-outline-secondary mx-2" href="/login">로그인</a>
                    <a class="btn btn-sm btn-outline-secondary mx-2" href="/register">회원가입</a>
                    {{/if}}
                </div>
            </div>
        </header>
    </div>

    <div class="container mt-5 d-flex justify-content-center">
        <div class="card">
            <div class="card-header py-2 h5 d-flex justify-content-center">
                회원 정보 수정
            </div>
            <div class="card-body">
                <form action="/auth/update" method="POST">
                    <div class="form-group">
                        <input type="hidden" name="authid" value="{{user.authid}}">
                        <label for="name">이름</label>
                        {{#if name}}
                            <input type="text" class="form-control" id="name" name="name" value="{{name}}" maxlength="25" required>
                        {{else}}
                            <input type="text" class="form-control" id="name" name="name" value="{{user.name}}" maxlength="25" required>
                        {{/if}}
                    </div>

                    <div class="form-group">
                        <label for="email">이메일</label>
                        {{#if email}}
                            <input type="email" class="form-control" id="email" name="email" value="{{email}}" readonly>
                        {{else}}
                            <input type="email" class="form-control" id="email" name="email" value="{{user.email}}" readonly>
                        {{/if}}
                    </div>

                    {{#if user.authid}}
                        <div class="form-group" style="display: none;">
                            <label for="password">비밀번호</label>
                            <input type="password" class="form-control" id="password" name="password" value="{{user.authid}}" readonly>
                        </div>
                    {{else}}
                        <div class="form-group">
                            <label for="password">비밀번호</label>
                            <input type="password" class="form-control" id="password" name="password" placeholder="비밀번호를 입력하세요." maxlength="20" required>
                        </div>
                    {{/if}}

                    <button type="submit" class="btn btn-primary mt-2 w-100">정보 수정</button>
                </form>
            </div>
        </div>

        {{!-- Register할 때, 메시지 발생했을 때 --}}
        {{#if message}}
            <h4 class="alert alert-danger mt-4">{{message}}</h4>
        {{/if}}
    </div>

    <footer id="sticky-footer" class="flex-shrink-0 py-4 bg-dark text-white-50 fixed-bottom">
        <div class="container text-center">
            <small>Copyright &copy; Hong Website</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
</body>
</html>